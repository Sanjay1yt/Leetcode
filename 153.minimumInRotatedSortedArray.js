var findMin = function (value) {
  return Math.min(...value);
};
console.log(findMin([3, 4, 5, 1, 2])); // this is not a method, actually in question we have (You must write an algorithm that runs in O(log n) time).
